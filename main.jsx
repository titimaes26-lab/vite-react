import React from 'react'
import ReactDOM from 'react-dom/client'
import RestaurantManager from './restaurant-manager.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RestaurantManager />
  </React.StrictMode>,
)
